# Connection string Postgres (dari tab Storage project Vercel, atau Neon/Postgres lain untuk
# dev lokal - lihat README.md bagian Deploy). Prisma butuh variant "pooled" kalau providernya
# mendukung connection pooling (mis. akhiran ?pgbouncer=true dari Neon/Vercel Postgres).
DATABASE_URL=""

# Generate dengan: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Pakai nilai BEDA untuk dev lokal vs produksi.
AUTH_SECRET=""
